# ── Local scripts (never push) ──
deploy.sh
make_thumbs.sh

# ── Environment & secrets ──
.env
.env.*
*.secret
*.key

# ── OS junk ──
.DS_Store
Thumbs.db
desktop.ini

# ── Editor metadata ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Logs & temp ──
*.log
tmp/
temp/

# ── Node (if ever used) ──
node_modules/
